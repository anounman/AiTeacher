# AiTeacher — TODO (v2, post-merge)

References [ARCHITECTURE.md](ARCHITECTURE.md) (§). Base = StudyGPT merge (2026-08-08) — its features are DONE and not re-listed. Rules: work in order within a phase; every task's **Done when** is the definition of done; deviations update ARCHITECTURE.md in the same change.

## Phase M — Merge hardening

- [x] **M.1** Adopt StudyGPT as base, keep our md specs, rename package `aiteacher`, npm install.
- [ ] **M.2** Boot verify: `npm run dev` serves chat; base unit tests (`test:fsrs`, `test:mastery`, `test:retrieval`) green; upload one PDF end-to-end with local Ollama.
  Done when: chat answers a question about an uploaded PDF in the browser.
- [ ] **M.3** Remove dead `@ai-sdk/react` dep. (`zod` is declared — that half is done.)
  Done when: `npm run build` green with the dep removed.
- [ ] **M.4** Test scripts cover every suite: `lib/grounding.test.ts`, `lib/persona.test.ts`, `lib/ingest/index.test.ts` exist and pass but are in no npm script, so nobody runs them. Add a `test` script globbing all `*.test.ts`.
  Done when: one command runs all suites (currently 34 assertions across 9 files) green.

## Phase 1 — Slots + provenance + enforced grounding + eval (the product)

- [ ] **1.1 Model slots** (§4) — *one piece left*: `config/models.json` + `lib/llm/slots.ts` exist with 6 slots (parse/dispatch/reason/**visual**/**read**/embed), env override `OLLAMA_<SLOT>_MODEL`, Settings override (2026-08-09, see 1.1b below), and `validateSlot()`. Remaining: `validateSlot` is never called outside `slots.ts`, so there is no health check reporting slot reachability.
  Done when: settings can override any slot and one command/route reports every slot reachable.
- [x] **1.2 Per-page ingest** (§7, 2026-08-09): per-page `unpdf` extraction; chunks get `loc={"page":n}` via migration; SourcesPanel snippets show page numbers. Page-boundary verifier added; the material route also accepts multiple plain-text/Markdown/CSV/JSON/code files.
  Done when: re-ingested PDF's citations show real page numbers; chunker unit test extended for page boundaries.
- [x] **1.3 FTS5 hybrid retrieval** (§6, 2026-08-09): `chunks_fts` migration + backfill/triggers; BM25∪cosine → RRF; stable envelope ids + `loc` attached to every excerpt. RRF, exact-keyword FTS, and live SQLite verifier are green.
  Done when: retrieval unit tests extended (RRF ordering, id passthrough) and a keyword-only query that cosine misses is retrieved via BM25 in a test.
- [ ] **1.4 Evidence envelope + nonce framing + injection rules** (§5): randomized delimiters, evidence-has-no-authority system rule, retrieval errors surface as SSE `error` (kill the silent `null`).
  Done when: a doc containing `</context>` and "ignore your instructions" text neither breaks framing nor changes behavior (manual trap doc committed to eval/gold).
- [ ] **1.5 Marker grounding + validator** (§5): `[S:id]` marker prompt rules exist and `lib/grounding.ts` already strips ids absent from the envelope (`sanitizeSourceMarkers`, 2 tests). Remaining: claim parsing/grading, leak counter, `claims` SSE event, `message_claims` migration, UI grounded/general chips wired to SourcesPanel.
  Done when: answers show clickable grounded chips; a forged marker (id not in envelope) is stripped and logged as handoff_leak.
- [ ] **1.6 Abstention gate** (§5): fused-score threshold from config; abstention system prompt path; `[general]` labeling rendered distinctly.
  Done when: asking about content absent from materials yields the abstention response with labeled general fallback, never a fake citation.
- [ ] **1.7 Gold set** (§10): ingest 2–3 real course PDFs; ~200 generated QA → hand-verify ≥100; 25 unanswerable + 5 injection traps; committed + versioned.
- [ ] **1.8 Eval harness** (§10): `eval/harness.ts`, suites `retrieval` + `grounding`, runs committed; baseline recorded.
  Done when: both suites produce metrics JSON on the gold set; abstention threshold tuned from sweep and recorded (task includes the sweep).
- [ ] **1.9 Latency spans** (§11): `data/traces.jsonl` per turn; `latency` suite over 20 canned turns.
  Done when: p50 first-token measured; misses documented with responsible span.
- [ ] **1.10 Vision parse stage** (§7): math-heavy page heuristic → rasterize → gemma4 → `chunks.latex`; `parse` suite with ~20 gold pages.
  Done when: a math PDF's equations are retrievable by LaTeX-ish queries; parse suite reports match rate.

## Phase 1a — Teaching quality and learner control

- [x] **1a.1 Plain-language teaching policy** (2026-08-09): chat, Feynman, document, and teach prompts default to short everyday language, define jargon immediately, use a concrete example first, and avoid unsolicited advanced detail.
- [x] **1a.2 Source-visible answers** (2026-08-09): evidence uses nonce-framed source ids; answers render inline source chips connected to page-aware SourcesPanel excerpts; invalid ids are stripped before persistence; empty evidence requires an explicit abstention.
- [x] **1a.3 Conversation teacher persona** (2026-08-09): beginner, Socratic, visual, exam-coach, and concise presets plus bounded learner context; available in chat and teach toolbars. Context is stored per conversation, server allow-listed, framed as untrusted preference data, and common prompt-injection/control attempts are rejected.
- [ ] **1a.4 Grounding quality gate**: finish Phase 1.5–1.8 claim persistence, calibrated abstention threshold, gold set, and eval harness; use results to tune retrieval instead of prompt intuition.

> **Reorder (2026-08-08, user decision):** teacher's primary reply mode = voice + board writing, not text. Phase 3 (canvas) + TTS pulled forward — now Phase 2. Conscious co-watch becomes Phase 2b, runs after. Grounding tasks (Phase 1) remain the product core and continue alongside.

## Phase 2 — Teaching mode: canvas + voice (PULLED FORWARD)

- [x] **2.0 Teach mode v1** (2026-08-08): `teach` conversation mode + ModeToggle; TEACH_SYSTEM_PROMPT (spoken/board fence protocol); `lib/teach/protocol.ts` stable-prefix stream parser + zod actions; TeachPanel orchestrator (speechSynthesis TTS + paced reveal, mute toggle); Board flow renderer (KaTeX, pages, box highlight, fade-in); board fences → transcript chips; split layout in page.tsx. Browser-verified: compiles, mode reachable. End-to-end lesson run still to be exercised.
- [x] **2.1 Handwriting renderer, stage 1** (2026-08-08): vendored calligrapher.ai engine (`public/handwriting/engine.js` + 2.6MB `d.bin`, driven headlessly via hidden sandbox DOM in `lib/teach/handwriting.ts`, single write queue, live park-in-host rendering); `StrokeText` writes prose/headings as real RNN pen strokes; `MathWriteOn` uses MathJax v3 browser build (`public/mathjax/tex-svg.js` — mathjax-full ESM hangs under Turbopack) with per-glyph dashoffset pen-trace + jitter; completion bus (`lib/teach/completion.ts`) keeps voice and pen in step; live-vs-history split (history renders typeset instantly). Browser-verified end-to-end: quadratic-formula lesson writes cursive heading + equations glyph-by-glyph with TTS. Hard-won bugs, do not reintroduce: engine svg must never be cleared externally (its rAF loop crashes; it self-clears on next write); idle detection needs ≥1.5s window (background-tab rAF throttling); StrictMode double-mount is deduped via `dataset.started` on the DOM node, and item effects run WITHOUT cancellation so completion signals always fire; embedding models are filtered from the chat model picker (selecting one 400s every turn).
- [x] **2.1b Equation marking** (2026-08-08, user request "mark out the equation like a human"): `mark` action in protocol (`target: "eqId#partId"`, styles circle/underline/box, colors red/blue/ink, handwritten label); model wraps sub-expressions in `\cssId{partId}{...}` (needs `tex-svg-full.js` — slim build autoloads the html extension from paths we don't serve); `MathMark.tsx` draws wobbly hand-style shapes over the target's bbox + writes the label via the stroke engine in the mark color; history latex now renders via MathWriteOn instant (KaTeX dropped from Board — it can't parse \cssId). Browser-verified: discriminant circled red + labeled, denominator underlined blue + labeled.
- [x] **2.1c Plan-then-perform + interruption + spatial index** (2026-08-09, user redesign):
  - Performance decoupled from generation: pump starts only when the stream completes; during generation every text/heading/label line is pre-synthesized offscreen (`synthesizeLine` cache, engine at speed 60) and MathJax warms — replay (`replayInto`) paces visible writing from cached strokes, killing the lag.
  - `lib/teach/performer.ts` store: pause flag (components poll between strokes/glyphs), per-message cursor progress, selection, onLessonDone. Whiteboard header gained pause/resume + region-select buttons.
  - Interruption: sending while performing pauses, ships `teachContext` `{lessonMd, deliveredUpTo, selection}` (route appends to system prompt: answer briefly, don't repeat lesson), answer performs as mini-lesson, `onLessonDone` restores the interrupted message as live and it resumes from its saved cursor (already-delivered events re-render instantly).
  - Spatial: `lib/teach/bvh.ts` (median-split BVH) + `lib/teach/spatial.ts` registry — equations, \cssId parts, per-character mi/mn/mo tokens, text lines, labels all registered; region query measures viewport rects at query time; `describeHits` ranks part > token > equation > text. Select-drag on the board → description chip → next question's context.
  - `npm run test:teach` — 5 unit tests (BVH exactness, parser stable-prefix invariant, fence validation, speakable). All green; tsc clean.
  - NOT yet browser-verified end-to-end: interrupt→answer→resume flow and select-to-ask (automation kept losing the conversation routing; needs a human run).
- [x] **2.1d Code action + no-shrink math** (2026-08-09): `code` board action (typed line-by-line, `data-part` lines markable as `c1:L0`, spatial `codeline` kind) — models were mangling C code through MathJax; MathWriteOn no longer shrinks long equations (container scrolls).
- [x] **2.1e mathwriter integration** (2026-08-09, user directive — friend's engine): vendored github.com/JayanshJ/mathwriter (`mathwriter/`, venv + `npm run writer` sidecar on :8931, `/api/handwrite` proxy); `write` action with mathwriter markup is now the board's primary pen (real harvested glyphs, fractions/integrals/roots/matrices, ink/red/blue recolor); `HandWrite.tsx` band-detects lines from PNG alpha, pen-wipe reveals them pausably, registers `id:L<n>` spatially, marks target lines; prompt v3 teaches the markup + renderer DO-NOTs. Verified in browser: handwritten heading + bounded integral + colored annotations on the board. Sidecar down → plain-text fallback. Note: `_recolor` must use `np.ascontiguousarray(...).copy()` (PIL frombuffer chokes otherwise); sidecar monkeypatches transparent paper + no scan effects.
- [ ] **2.2 Stage 2 (optional, RunPod)**: stroke server (`{text, style} → strokes JSON`) self-hosted; client falls back to in-browser weights when absent.
- [ ] **2.3 Teach-mode e2e check (user)**: full lesson; interrupt mid-lesson with a question (watch it pause + resume); mark a region with ▭ select and ask about it; record feel verdict + pacing constants.

## Phase 2b — Conscious co-watch

- [ ] **2.1 MCP client** (`lib/conscious/`): boot spawn, backoff, tools `conscious_recall`/`conscious_ocr_at` in chat route's tool set; visible degradation string.
  Done when: with Conscious up, a debug route round-trips recall; with it down, chat stays healthy and says so when asked to use it.
- [ ] **2.2 Video materials + player**: `source_type='video'`, upload/link, player page, throttled `player_state` → `playback_offsets`.
- [ ] **2.3 Pause-and-ask**: cowatch envelope injection (transcript window + `ocr_at(t)`, ids `v<mat>@<t>`); `video@mm:ss` citations clickable → seek.
  Done when: pausing and asking "what did she just say about X" answers with a ±10s-accurate timestamp citation.
- [ ] **2.4 Co-watch eval suite**: 20 gold pause-and-ask questions on one lecture; metrics committed.
- [ ] **2.5 Tool-call reliability check**: if reason-slot native tool calls misfire (>10% malformed on 30-intent gold), add dispatch-slot formatting layer per §8; else record the measurement and skip.

## Phase 2c — Live-lesson UI redesign (Stitch, 2026-08-09)

Target design: [design/live-lesson-stitch.png](design/live-lesson-stitch.png) (+ HTML reference `design/live-lesson-stitch.html`; Stitch project `projects/18219170476883645500`, design system "Academic Intelligence"). Decisions it encodes: infinite zoomable edge-to-edge canvas (≥90% of viewport, subtle warm graph paper, earlier lesson content faded at reduced zoom); ALL controls as frosted floating overlays — top-center toolbar pill (pause/voice/select/new-page + "writing…" pen status), right collapsible Lesson Transcript card (spoken bubbles + Board Step chips, edge chevron to hide), bottom-center "Ask anything — the lesson pauses for you" input pill with mic, bottom-right zoom %/fit + minimap, top-left tiny breadcrumb. **Handwriting size hierarchy (fixes current sizing): headings 44–52px, equations 30–36px with ≥1.5x line spacing and wide left margin, annotations/labels 20–22px, red = emphasis, blue = secondary.**

- [x] **2c.1 Canvas shell** (2026-08-09): `lib/teach/canvas.ts` — world transform `translate/scale`, wheel pans, ctrl/⌘+wheel (and pinch) zooms at cursor, pointer drag pans, zoom in/out/fit buttons, auto-follow that keeps the newest item in view and yields the moment the student pans. Board rewritten as world content (no page cards, no inner scroller); `new_page` becomes a faint divider further down the infinite board.
- [x] **2c.2 Overlay chrome** (2026-08-09): `components/teach/TeachStage.tsx` replaces TeachPanel and takes the whole viewport when `mode === "teach"` (page.tsx early-returns it). Frosted `.glass` overlays: breadcrumb (click = back to chat mode), toolbar pill (pause/voice/select/fit + animated writing status), right transcript card (collapsible via edge chevron; assistant turns render as spoken prose + "N board steps" chips), bottom composer pill (Web Speech push-to-talk, send/stop, selection chip), zoom pill.
- [x] **2c.3 Handwriting scale pass** (2026-08-09): `WriteRole` heading/equation/annotation → mathwriter scale — first calibrated at 1.8/1.25/0.85, **since reduced to 1.45/1.0/0.72** (see 2c.12; the first pass read as oversized on a real board). The PNG renders at natural pixel size — the old `width:100%` stretch was what flattened every item to the same size. `roleFor()` infers role from `~~heading~~` / color. Mark labels moved off the RNN onto mathwriter (`writeLabel`) — fixes the garbled "innur"/"mumber" labels.
- [x] **2c.4 Bug: interruption never resumed** (2026-08-09): asking mid-lesson paused the performer, but the answer's own performance then sat paused forever — `runChat` now resumes on setting a new `teachLiveId` (the old pump exits on the liveKey check first, so this is safe).
- [x] **2c.5 Kokoro voice** (2026-08-09, user directive): `/api/tts` proxy (GET lists voices, POST streams mp3; server-side so the Tailscale host and CORS stay out of the browser), `lib/teach/tts.ts` client with per-segment cache, prefetch during plan generation, pause-in-place/resume-mid-word, and speechSynthesis fallback when Kokoro is down. Toolbar voice picker (68 voices, stored in `localStorage["teach.voice"]`, default from `config/models.json` → af_bella). Browser-verified: blobs play without autoplay block; paused at 4.87s, held, resumed to 6.04s.
- [x] **2c.6 Voice↔pen sync + camera + margin asides** (2026-08-09, user feedback "not synced, not focused, re-explain at the mark"):
  - **Beats**: the pump now performs each spoken run and its board fence TOGETHER (speech task ∥ draw task, `Promise.all` per beat) — the teacher talks while the pen moves, instead of speak-everything-then-write. No second model needed: the lesson plan's segment/fence interleaving already encodes what narrates what. Cursor advances per beat, so resume replays at most one beat.
  - **Camera**: `canvas.focus()` tweens (450ms cubic ease) to each item as it's written; manual pan/zoom suppresses it for 6s then it re-engages; `force` override for revisits. Verified: 6 distinct camera moves during one lesson, writing appearing while audio played.
  - **Margin asides**: marquee selection now captures the mark's world rect (`performer.setSelection(desc, rect)` → `anchorFor(lessonId)`); an answer to a marked question renders its board items in a `.board-aside` absolutely positioned in the free space LEFT of the mark (red tick border), and the camera force-focuses there first. Route prompt tells the model its writing lands as a compact margin note. Verified end-to-end: "explain this marked part again, simpler" → aside at left:-458px beside the marked line with 2 handwritten items, camera moved back. Limitation: anchors are session-memory only — on reload an anchored answer re-renders in the main column (persist anchors in DB if it matters).
- [ ] **2c.6b** Minimap (deferred — zoom + fit + auto-follow cover the need; add only if navigation feels lost).
- [ ] **2c.7** Board world is a fixed 900px column; wide code/equations can overflow it. Revisit if content clips.
- [x] **2c.8 Multi-model visual director** (2026-08-09): dedicated Ollama `visual` slot (`qwen3.5:latest`) receives the completed reason-slot event stream and is forced to call `draw_architecture`. The model selects only supplied board ids/relationship pairs; labels and edge meaning are copied deterministically from the original lesson, then semantically validated. Stable v1 names: `place_asset`, `write_label`, `draw_arrow`, `draw_architecture`, `arrange_layout`, `group_elements`, `emphasize`; future dataset keys live in `assetId`. `visual_scene` renders responsive SVG architecture plus dataset-ready placeholders; result persists into the message. Model runs beside playback, warms on teach-mode entry, and failure/timeout returns a fact-preserving deterministic plan.
  Done when: one endpoint can direct a sample lesson with a typed tool call; every returned action validates; original speech is byte-for-byte preserved; visual actions render; and an unavailable visual model does not delay or break teaching mode.
- [x] **2c.9 Continuous performance timeline** (2026-08-09): `lib/teach/timeline.ts` (`buildLessonTimeline`, `estimateSpeechDurationMs`) replaces the beat-wide `Promise.all`; `TeachStage` drives cues from it with pause-aware monotonic waits, speech as the real clock, bounded handwriting settle windows, and one shared event cursor for transcript highlight, board and camera. Covered by `lib/teach/timeline.test.ts`; `npm run test:teach` green (27 assertions incl. expression + visual director + TTS route).
  Manual feel gate still owed — tracked in 2c.11.
- [x] **2c.10 Expressive teaching voice** (2026-08-09): stable provider-neutral expression names (`neutral`, `warm`, `encouraging`, `curious`, `excited`, `serious`, `reassuring`) plus restrained pace/rate inference. Expression and rate are in the TTS cache key; Kokoro receives only supported speed/volume controls; browser speech synthesis maps rate/volume/pitch. Legacy payloads remain valid and 7 focused tests cover inference + route validation.
  Done when: expression/rate participate in the TTS cache key, invalid cues are rejected, legacy TTS payloads still work, and tests verify deterministic cue inference plus provider mapping.
- [x] **2c.12 Dark-mode ink + size calibration** (2026-08-09, user report "in dark mode the writing is not visible and the size of writing is too big"): main ink is now requested from the sidecar as a stable dark raster and theme-adjusted at paint time, so a theme switch after prefetch can't leave invisible strokes (accent red/blue still use theme tokens); `ROLE_SCALE` reduced to heading 1.45 / equation 1.0 / annotation 0.72.
  Still to confirm by eye: dark mode across board item types (code block, mark labels, visual scene) — fold into the 2c.11 pass.
- [~] **2c.11 Multi-model feel gate** (first run 2026-08-09, hash-table lesson after the rate-limit reset — instrumented in-browser):
  - **Sync**: 4 of 5 board items appeared while lesson audio was playing; 12 distinct camera moves. Timeline pacing works.
  - **Themes**: dark AND light verified by screenshot across handwriting, hand-drawn `[T]` table, red/blue annotations, visual-scene card — the 2c.12 ink raster holds in both. (A mid-repaint screenshot briefly looked like stale dark bubbles; a settled frame was correct.)
  - **Visual director**: scene rendered but from the **deterministic fallback** — `qwen3.5` did not deliver a valid `draw_architecture` call in time (toolbar chip "visual fallback"). Fallback path behaved exactly as designed; the model path needs its own investigation: check `/api/teach/visualize` latency vs the cold-start warm request, and whether the plan failed validation vs timed out.
  - **Interrupt**: mid-performance question ("which one does Python dict use?") paused the lesson and produced a correct, brief spoken answer (open addressing + probing). Speech continued after the answer — resume-under-timeline looks right, but only an ear can confirm the interrupted lesson's remaining segments actually replay in order: **user, run one interrupt with sound on**.
  - Still owed for [x]: user's feel verdict on pacing (write-speed vs speech), the visual-model latency numbers, and the qwen fallback investigation above.
- [x] **2c.13 Diagram engine upgrade** (2026-08-09, from github.com/anounman/mathwriter-diagrams): vendored `render.py`/`charset.py`/`validate.py` upgraded, `draw_engine.py` + `diagrams.py` + `DRAW_REFERENCE.md` added (glyphs unchanged). `write` markup now supports `[G]{json}[/G]` auto-layout diagrams (tree/array/dp_table/linked_list/graph/stack/queue/memory/pointer/knapsack) and `[DRAW]` freeform primitives with hand tremor. Server recolor preserves HIGHLIGHT yellow. Teach prompt teaches both forms. Verified: mixed text+DRAW and [G] renders through the live sidecar; `npm run test:teach` green.
- [x] **2c.15 Visual fallback storm + flat voice** (2026-08-09, user report "so many visual fallback… voice still not emotional"). Four separate causes, all fixed:
  1. **`visual` slot ran `qwen3.5:latest` locally on a memory-starved Mac** — the model sat loaded on GPU but never answered (>8 min for a one-word prompt), so every director call timed out into the fallback. Slot repointed at `nemotron-3-nano:30b-cloud`; `ollama stop` freed the wedged local model. The slot pick is config, so `visual-director.test.ts` no longer pins a model name — it asserts the invariants (distinct from `reason`, ctx ≥ 8192).
  2. **The fallback itself threw.** Board labels are raw `[T]`/`[G]` markup, far over the plan schema's 100-char cap, so `visualPlanSchema.parse` rejected inside `fallbackVisualPlan` — and because that call sits in `planLessonVisuals`'s catch block, the throw escaped and 500'd the route. Strings are now clamped where lesson text crosses into a plan (both fallback and model paths), and the catch has a last-resort `emptyVisualPlan` so the fallback can never propagate.
  3. **Fallback scenes are redundant since 2c.13.** The deterministic card only restates elements the lesson already drew by hand, and its summary ("Deterministic visual fallback derived only from supplied lesson elements") was developer-speak on a student's board. Fallback plans are no longer rendered *or* persisted — only a real model plan earns a scene. Director timeout cut 45s → 20s, since a scene landing after the lesson ends is worthless.
  4. **Voice expression spread was inaudible.** Kokoro exposes no emotion control, so speed/volume *are* the delivery — but the profiles spanned only ±8% rate / ±4% volume, under the just-noticeable difference, making all seven expressions sound identical. Widened to 0.76–1.22 rate / 0.90–1.15 volume (well inside Kokoro's 0.25–4 clamp), with a test asserting the spread stays ≥ 0.3 so it can't silently regress.
  Verified end to end: a live binary-search lesson reported `visuals ready` (source `model`) — the director's first non-fallback run.
- [ ] **2c.16 Director abort signal is not honored**: `planLessonVisuals` ran 439s and 545s against a 60s `AbortSignal.timeout` before returning "aborted due to timeout" — the ollama provider appears to ignore `abortSignal` mid-generation, so the wall-clock cost is paid regardless of the cap. Harmless now (playback never waits on it) but it burns a cloud request per lesson.
  Done when: a director call with a 20s budget actually returns control at ~20s.
- [x] **2c.17 Teach mode ignores the `reason` slot** (2026-08-09): the teach route used `conv.model` — whatever the chat dropdown last held — and the teach UI has no model control, so a lesson silently inherited a model unfit for authoring (`glm-5.2:cloud` spiralled re-deriving arithmetic and never emitted board content). Teach turns now resolve through `resolveSlotConfig("reason")`; the chat picker governs chat mode only. Verified by running a lesson on a conversation whose stored `model` is still `glm-5.2:cloud` — it generated board content normally.
- [x] **1.1b Per-slot model override in Settings** (2026-08-09, closes the "give the option to change the model" ask): resolution order is now call-site → `OLLAMA_<SLOT>_MODEL` → Settings choice → `config/models.json`, with env above Settings so a deployment can pin a slot the UI cannot undo. `slot.<name>.model` keys live in the existing settings KV store — no new persistence. `GET/PATCH /api/settings` carry a `slots` array reporting each slot's effective model, the checked-in default it overrides, and whether env has pinned it (the UI disables those rows). Settings page gained a "Capability models" fieldset: one dropdown per slot listing installed models, each labelled with what that capability does and what it currently resolves to; the empty option clears the override. `/api/models?all=1` keeps embedding models in the list, since `embed` needs them while the chat picker must never offer one. A saved pick the backend no longer lists stays selectable so saving an unrelated field cannot silently reset it.
  Still open from the original 1.1: the health-check route.
- [x] **2c.14 Visual director → hand-drawn diagrams** (2026-08-09): `lib/teach/visual-draw.ts` converts a validated `draw_architecture` plan into `[DRAW]` markup (deterministic layered layout; edge labels dropped on arrows too short to fit them), and `VisualScene` renders architecture-only plans as a bare `HandWrite` item — same hand as the board, spatially indexed, no card chrome. The SVG card remains only for future asset-catalog action types. Verified by rendering a 6-node half-adder plan through the sidecar.
- [x] **2c.28 Teaching-style analyzer + Abdul Bari board craft** (2026-08-09, user ask "replicate the way he teaches"): built `scripts/analyze-teaching-video.mjs` — a reusable pipeline (yt-dlp → ffmpeg frame every N s → `read`-slot vision description per frame → reason-slot synthesis) that turns any lecture video into a grounded teaching-technique report (`scripts/out/<id>/report.md`, claims cited to timestamps, frames kept beside it). Conscious MCP identified the video the user was watching (Abdul Bari, "Introduction to Backtracking", DKCbsiDBN6c — confirmed by the user's link); ran 20 frames. Distilled findings → new **BOARD CRAFT** section in TEACH_SYSTEM_PROMPT: cumulative board (never erase), underlines only for titles/headers, declare the cast once, a permanently-visible boxed concrete anchor (the 3 chairs + n=3 3!), pre-staged section headers before structures, trees grown branch-by-branch in narration order with choices on edge labels, structure-first-verdicts-second (X "killed" / ✓ overlays as a second pass), comparisons as side-by-side columns reusing the same diagram, and the setup → naive walk → pruning insight → generalization arc. Report + frame notes vendored under `scripts/out/DKCbsiDBN6c/`.
- [x] **2c.25 iPad pen: GoodNotes gesture model + ink quality** (2026-08-09, user report "when writing mode is on I can't drag — one finger writes, two fingers should pan"): unified pointer router in TeachStage — **Apple Pencil always writes** (any mode, current color); in pen mode a single finger writes too, but a **second finger landing cancels the young stroke (<600ms) and turns both fingers into pan + pinch-zoom** (the GoodNotes disambiguation); mouse draws in pen mode, pans otherwise. `useCanvas` gained `panBy`/`pinchAt` primitives (both mark manual navigation). Ink upgraded to **perfect-freehand** (steveruizok, MIT): pressure-shaped outline polygons instead of constant-width polylines, coalesced pointer events for smooth fast strokes, `pointercancel` handled. **Eraser** in the pen controls (`ink_eraser` icon): rub to remove whole strokes (point-radius hit, world-scaled), spatial index unregistered on erase/undo/cancel (`unregister()` added to spatial.ts).
- [x] **2c.26 Verified: teach → assign → pen answer → teacher check** (2026-08-09, live run): asked for a linear-equations lesson; the tutor taught 2x+3=11 with red worked marks, then **on its own** wrote "Try: 3x − 5 = 10" and said "solve it on the board with the pen." Wrote "x = 5" in ink, tapped check-my-work → minimax read the strokes exactly → teacher replied "That's correct — nice work!", **wrote the verification on the board** (`Check: 3(5) - 5 = 15 - 5 = 10 ✓` + blue "x = 5 is right"), circled the question, and offered a follow-up. The complete human-teacher loop.
- [x] **2c.27 Settings → Configuration page** (2026-08-09): full redesign — sticky section rail (Connection / Capability models / Learner profile / Connected apps / API keys), page-card sections, two-column grids, sticky save bar, token count in the rail. All prior state logic kept.
- [x] **2b.0 MCP connections + Conscious live** (2026-08-09, first Phase-2b milestone): the app is now an MCP **client**. `lib/mcp/client.ts` (@modelcontextprotocol/sdk): per-server cached connections (stdio + streamable-HTTP with SSE fallback), tools namespaced `<server>_<tool>`, bounded timeouts, dead servers contribute nothing. Config in settings KV (`mcp.servers`), validated server-side (it can spawn local processes — shapes are whitelisted). Settings → Connected apps: add/toggle/remove servers + one-tap **Connect Conscious** preset (`node ~/Code/Conscious/daemon/mcp-server.mjs`). Chat route offers MCP tools every turn (merged with web_search, stepCountIs(5)) with a system-prompt note to cite tool-fetched facts. **Verified e2e:** all 5 Conscious tools load in ~1s; a chat turn asked "what was I working on in the terminal today" → `conscious_recall` → answer contained the real captured session (daemon command, osascript automation, localhost:8080 error, the Abdul Bari backtracking video playing right then). Still open for full 2b: session binding (timerange ↔ resource node), pause-and-ask with `ocr_at`, `video@t` citations that seek a player.
- [x] **2c.24 Cloud vision found — `read` slot** (2026-08-09, user ask "pick any vision model available in Ollama cloud"): the cloud catalog does have vision models — `minimax-m3:cloud`, `qwen3.5:cloud`, `kimi-k2.6:cloud` all pull and accept images (`qwen3.5:27b/122b-cloud` and `mistral-large-3:cloud` tags don't exist; earlier "no cloud vision" finding was true only of the models then pulled). Raced all three on rendered handwriting: every one transcribed exactly (incl. multi-line quadratic factoring), **minimax-m3 fastest (1.5–3.7s)**. New 6th slot `read` (default `minimax-m3:cloud`) — `/api/teach/read-ink` now uses it (was parse/gemma4), pen-pickup warm-up deleted (cloud is always resident), timeout 45s→20s. `parse` stays gemma4 local for offline ingest. Settings picker shows the slot automatically. Verified e2e: route transcribes "Sum = 1, carry = 0" in 1.7s. **Unblocks** the vision stage of the repair loop (2c.23) — needs a DOM-capture path for board screenshots before wiring. (2026-08-09, user report "text overlapping, diagram parts overlapping — build a repair layer with a vision model"). Two findings first: (a) **root cause of the overlap** — margin asides were placed at `anchor.x - 460`, which lands ON the 900px column whenever the mark sits within 460px of its left edge; asides now live in the true margin (`left: -470`). (b) **No configured model has vision except local gemma4** — nemotron/minimax/glm/deepseek cloud all return "does not support image input" (tested), so the requested nemotron-vision repair/read is impossible today, and a gemma-based loop would gate every lesson on a ~45s cold load. The repair layer is therefore **geometry, not vision** — the user's own observation ("every character has a unique position in the matrix") is exactly why vision is unnecessary: `lib/teach/repair.ts` measures live rects (world units), `planShifts` slides each aside below every blocker deterministically, applied as an eased transform that React re-renders can't clobber, re-derived from scratch each pass (idempotent). Runs on a 900ms beat in TeachStage — handwriting PNGs land async, so late arrivals get repaired too. 5 unit tests + verified live: an injected aside overlapping the column got `translateY(195px)` on the next tick. **Still open:** mark-label collisions (small, annotation-sized) and true visual QA (clipped/ugly rendering) — gated on a fast vision model becoming available; revisit when Ollama cloud ships one.
- [x] **2c.22 Engine sync #2** (2026-08-09, `mathwriter-diagrams@47da959`): real engine changes this time — `diagrams_extra.py` (599 lines, 12 fixed `[G]` types: `logic_gate`, `logic_circuit`, `er_diagram`, `relational_schema`, `sql_join_venn`, `mapreduce`, `cap_theorem`, `database_sharding`, `consistent_hashing`, `hdfs_architecture`, `kafka_pipeline`, `spark_lineage`), render.py dispatch for them + str-spec fallback, diagrams.py joint-fix. Vendored; `//` comment patch re-applied to `draw_engine.py`. **Quality note:** upstream `logic_gate`/`logic_circuit` render rough (malformed XOR body, truth table as raw tuples, `outputs` requires x/y despite docstring, wires miss gate inputs) — the teach prompt keeps steering gates to the `[DRAW]` SHAPE RECIPES, which render visibly better; revisit when upstream matures.
- [x] **2c.21 Engine sync — DRAW example library + shape recipes** (2026-08-09, `anounman/mathwriter-diagrams@d344904`): the repo was force-pushed but **every engine file is byte-identical** to what 2c.13 vendored (`render.py`, `draw_engine.py`, `diagrams.py`, `charset.py`, `validate.py`, all 749 glyphs) — the real change is 17 new worked `[DRAW]` examples plus generator prompts, including proper logic-gate symbols. Vendored all 19 into `mathwriter/examples/` (76 KB, each verified to render) and distilled them into a SHAPE RECIPES block in the teach prompt: AND as a D-shaped POLYGON, OR/XOR from CURVE+CUBIC arcs, NOT/NAND inversion bubbles, wire forking, Venn and ER idioms — plus the previously undocumented `CUBIC`/`BRACE`/`BRACKET`/`PATH` primitives. Fixes the half-adder complaint at the source: the model was drawing a labelled `RECT` because no prompt text told it a gate has a shape. Verified by rendering a half-adder built purely from the recipes — real XOR double-curve, D-shaped AND, forked inputs. One local patch to `draw_engine.py`: `//` comment lines are now skipped explicitly (upstream strips only `#`).
- [x] **2c.20 Hermes-style self-improvement loop** (2026-08-09, user ask: "after every prompt it should improve itself, learn my study style" — pattern from github.com/NousResearch/hermes-agent). `lib/learner/profile.ts`: after each chat/teach turn's response is sent, a fire-and-forget reflection on the **dispatch slot** reads a turn *digest* (not a transcript replay — Hermes' cost lesson), decides if the exchange revealed anything durable about HOW the student learns, and rewrites a char-capped (3.2k) learner profile in the settings KV. The profile is prompt memory: injected into every chat/teach system prompt with the same untrusted-data nonce framing as the persona block — style/pacing/format only, never grounding or protocol. Guards: micro-turns skipped without a model call, off-format/chatty replies can't clobber the profile (bullet-lines only), imperative injection lines dropped, failures silent (next turn reflects again). Settings shows the profile as an editable textarea with "forget everything". 5 tests in `lib/learner/profile.test.ts`, wired into `test:teach` (33 green). Verified live: one pointers question ("I hate long text walls, I learn from diagrams") → profile learned diagram-first/short/real-addresses preferences autonomously.
  Deliberately NOT ported from Hermes: skill files, session-archive search, Honcho user modeling — the concept graph + mastery + FSRS already cover the knowledge side; this loop covers style.
- [x] **2c.19 Transcript typography** (2026-08-09, user report "transcript font looks really bad"): the transcript inherited the page's Newsreader serif, which reads poorly as short dense bubbles in a 340px column. Bubbles and the panel heading now use a native UI sans (`--font-ui`, system stack — no extra font download), with slightly looser line-height, a touch of negative tracking, and `text-wrap: pretty` to stop orphans. The board, chat prose, and mono chrome are untouched; serif remains the reading voice, sans is only for chrome.
- [x] **2c.18 Teaching-quality prompt pass** (2026-08-09, user report: truth table drawn as flat comma text, no worked calculations, content scattered). TEACH_SYSTEM_PROMPT now (a) shows the exact `[T]` cell/row syntax with a half-adder example — the old prompt named the tag but never showed its syntax, which is why the model emitted CSV into a box; (b) mandates SHOW THE WORK (tables built row by row, formulas applied to concrete numbers, traces with `[X]old[/X] new`); (c) mandates BE VISUAL FIRST (structure → `[G]`/`[DRAW]` diagram, rare to have none); (d) bans blank lines inside markup and asks for adjacent related items to keep the board tight. Verified live: half-adder lesson produced a proper hand-drawn truth table, red per-row sums beside it, both logic equations, a hand-drawn gate circuit, and marks labelling the XOR/AND gates — single tight column.

## Phase 3 — Canvas output (teacher draws) — ~~SUPERSEDED by Phase 2/2c~~

Kept for the record only. tldraw was never adopted (not a dependency); the board became our own infinite canvas with mathwriter handwriting, a cue timeline, and a following camera. The one genuinely unfinished idea from this phase:

- [ ] **3.2 Board persistence**: `board_pages`/`board_items` migrations so a lesson's board (and margin-aside anchors, currently session-only) survives reload.
  Done when: reloading a finished lesson restores the board, including anchored asides.
- ~~3.1 tldraw pane~~ / ~~3.3 teach_actions SSE~~ / ~~3.4 feel gate~~ — done differently in 2.0–2c.10 (feel gate now 2c.11).

## Phase 4 — Canvas input (student's pen — iPad is the target device)

- [x] **4.0 Ink layer + word-level matrix + go-back** (2026-08-09, built while backend was rate-limited — none of it needs the LLM):
  - Pen tool on the toolbar (`components/teach/InkLayer.tsx` + `useInk`): freehand strokes in world coordinates (pan/zoom with the board), red/blue/ink swatches, undo, pressure-modulated width. Apple Pencil arrives as `pointerType:"pen"` pointer events — same code path as mouse; `touch-action:none` while inking.
  - Every stroke registers in the spatial index (kind `ink`) so marquee selection resolves "the student's own pen drawing".
  - **Word-level ids**: `HandWrite` now splits each line band on horizontal ink gaps and registers `writeId:L<n>:W<m>` boxes (kind `word`), text paired in order with the markup's words. Verified: selecting one word yields `the word "where" in f2`.
  - **Go back**: history board entries are keyed per message (`b-<msgId>-<i>`); clicking a transcript turn force-focuses the camera on that turn's board items. Verified in browser.
  - Limitations, deliberate: ink is session-only (persistence with 3.2); word→text pairing is order-based best effort; ink is not yet read by any model.
- [~] **4.1 Ask-pen — answer checking shipped** (2026-08-09, user ask "AI should fetch my pen answer and check it like a normal person"):
  - `lib/teach/ink-capture.ts` rasterizes the ink strokes to a white-backed PNG (≤2x scale, capped bitmap); `/api/teach/read-ink` reads it with the **parse slot's vision model** (gemma4) and returns a faithful transcription. White background matters: transparent PNGs read as black-on-black and come back UNREADABLE.
  - "check my work" button in the pen controls (appears with the first stroke): capture → spatial context via BVH (which board items the ink sits next to, ink hits excluded) → vision transcription → the existing interruption flow, where the reason model judges the answer with the whole lesson in context and speaks back. Unreadable ink degrades to a polite "couldn't read it, write clearly or say it" turn.
  - Cold-start: picking up the pen fires a tiny warm-up read so gemma4 (~45s cold) is resident by the time an answer is written; warm reads ≈2s.
  - Teach prompt now teaches interactively: write a one-line exercise on the board every 3–4 ideas, invite pen or voice answers, judge transcribed handwriting on the math not the penmanship, nudge instead of restating.
  - Verified live: readable path transcribes exactly ("Sum = 1, carry = 0"); unreadable path produced a natural teacher retry-invitation, spoken.
  - Still open from the original 4.1: circling *board content* and asking about it (today the vision read covers the student's own ink; region snapshot incl. board pixels is the remaining piece), ink persisted with its reading.
- [x] **4.7 Note export** (2026-08-09, user ask "export the whole note into GoodNotes"): sidecar gains `POST /render_pdf` (mathwriter's own page renderer — grid paper, no scan effects, in-memory); `/api/teach/export?conversationId=…` rebuilds the lesson's write/code/heading actions into one markup document and streams back `<title>.pdf`; download button in the teach toolbar. Handwriting re-warps on export — which is what handwriting does. Verified: half-adder lesson exported as a one-page blue-ink notebook PDF. Student ink and mark overlays not included (need 3.2 board persistence for faithful geometry).
- [ ] **4.2** Annotation mini-eval: 15 gold snapshots, ≥80% intent accuracy before shipping.
- [ ] **4.3** Voice→turn loop: base voice input already transcribes into ChatInput — verify path + add latency span. (Mostly done by base.)
- [ ] **4.4 Exercise loop — teacher assigns drawing** (user vision, e.g. knapsack): protocol gains an `exercise` action ({prompt, expected_kind: table|graph|ink}); performance pauses at it, composer shows "your turn — draw it"; student draws (ink and/or a grid the teacher pre-writes); on "done", parse-slot reads the snapshot + spatial context and the reason model critiques/continues ("now eliminate from the last row, what's the recurrence?"). Needs 4.1's vision reading; spec before building.
  Done when: a knapsack lesson asks for a table, reads the student's filled cells, and corrects one wrong cell by pointing at it.
- [ ] **4.5 iPad pass**: Safari/iPadOS gesture audit (pencil vs finger: pencil draws, finger pans), overlay hit targets ≥44px, `apple-mobile-web-app` meta, test on device.
- [ ] **4.6 Dataset prep (user, ongoing)**: user is assembling a drawing/writing dataset so the visual model can draw richer scenes; `lib/teach/visual-schema.ts` action names are the stable contract — concrete artwork keys live in `assetId` only.

## Phase 5 — Graph & consolidation (eval-gated)

- [ ] **5.1** Gate: hybrid retrieval recall@8 on gold. <0.85 → 5.2; else record no-go here.
- [ ] **5.2** (gated) One-hop `concept_edges` expansion in retrieval; keep only if recall rises without citation-precision loss.
- [ ] **5.3** Prerequisite-aware teaching: `graph_neighbors` tool over `prerequisite_of` edges; teacher sequences explanations; qualitative user check.
- [ ] **5.4** Consolidation endpoint: link Conscious lecture episodes to concept nodes.

## Parking lot

LaTeX→ink model trained on MathWriting (handwriting stage 3 — real ML project, RunPod, see ARCHITECTURE §8); iPad/PencilKit; streaming STT/VAD; sqlite-vec/ANN index; job queue for ingest; GraphRAG libs; multi-user/auth; refactor of `app/page.tsx`/`ChatInput.tsx` monoliths (only when a task forces entry).
