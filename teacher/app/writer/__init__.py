"""Writer Engine client. The engine itself is a separate project.

This package holds the versioned HTTP client, a mock implementing the same
contract, and the contract test both must pass. See ARCHITECTURE_V2 §5.
"""
