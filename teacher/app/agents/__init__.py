"""Plane B — Teaching. Evidence in, a lesson out.

One deepagents root (`teacher`) with isolated subagents: researcher,
board_director, examiner, reflector. Grounding is NOT enforced in here — a
validator outside this package drops any claim whose citation is absent from
the evidence envelope. Agents are not trusted to police themselves.
"""
