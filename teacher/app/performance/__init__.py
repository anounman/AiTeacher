"""Plane C — Performance. A lesson in, a synchronized cue stream out.

Speech is the master clock; handwriting gets a bounded settle window. No plane
may gate the lesson: a failed write renders as text, a cold model renders a
deterministic fallback.
"""
